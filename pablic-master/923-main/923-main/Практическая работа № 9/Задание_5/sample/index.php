<?php
// Установка кодировки UTF-8 для корректного вывода кириллицы
header('Content-Type: text/html; charset=utf-8');

// Исходный массив из задания
$array = ["Мастер и Маргарита", "М.Булгаков", 1940, "Мистика", true];

// 1. Вручную преобразуем массив в строку JSON (в виде JSON-массива)
// Строки оборачиваем в двойные кавычки, числа и булевы значения оставляем без кавычек
$json_string = '["Мастер и Маргарита", "М.Булгаков", 1940, "Мистика", true]';

// 2. Декодируем представление с помощью функции json_decode
$decoded_result = json_decode($json_string);

// 3. Выводим результат декодирования в браузер
echo "<h3>Результат успешного декодирования JSON:</h3>";
echo "<pre>";
print_r($decoded_result);
echo "</pre>";

// Проверка на корректность декодирования
if (json_last_error() === JSON_ERROR_NONE) {
    echo "<p style='color: green;'>✔ Строка JSON записана верно и успешно декодирована!</p>";
} else {
    echo "<p style='color: red;'>❌ Ошибка в синтаксисе JSON: " . json_last_error_msg() . "</p>";
}
?>
